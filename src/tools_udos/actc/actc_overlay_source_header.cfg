MEMORY
{
  CODE:
    start  = $A000,
    size   = $2000,
    file   = %O,
    define = yes;
  BSS:
    start  = $8000,
    size   = $2000,
    file   = "",
    define = yes;
}

SEGMENTS
{
  CODE: load = CODE;
  BSS:  load = BSS, type = bss;
}
